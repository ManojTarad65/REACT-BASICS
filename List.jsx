import React from "react";

const List = () => {
  const names = [];
  const age = 22;
  return (
    <div class="
    />">
   
      {names.map((name, index) => (
        <div >
        

          <p>{name}</p>
          <p>{name}</p>
        </div>
      ))}
    </div>
  );
};

export default List;